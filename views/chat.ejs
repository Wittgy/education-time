<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat Uygulaması</title>
    <script src="/socket.io/socket.io.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const socket = io();
            let username;
    
            // Kullanıcıdan adını iste
            while (!username || username.trim() === "") {
                username = prompt("Lütfen bir kullanıcı adı girin:");
            }
            
            socket.emit("new user", username); // Kullanıcıyı sunucuya bildir
    
            const messageForm = document.getElementById("chatForm");
            const messageInput = document.getElementById("msgInput");
            const messageList = document.getElementById("messages");
            const userList = document.getElementById("userList");
            const typingIndicator = document.getElementById("typingIndicator");
    
            messageForm.addEventListener("submit", (e) => {
                e.preventDefault();
                let msg = messageInput.value.trim();
                if (msg) {
                    socket.emit("chat message", { message: msg });
                    messageInput.value = "";
                    socket.emit("stop typing"); 
                }
            });
    
            messageInput.addEventListener("input", () => {
                if (messageInput.value.trim() !== "") {
                    socket.emit("typing"); 
                } else {
                    socket.emit("stop typing"); 
                }
            });
    
            // ✨ Gelen mesajları ekrana yazdır
            socket.on("chat message", (data) => {
                let li = document.createElement("li");
                li.innerHTML = `<strong>${data.user}:</strong> ${data.message}`;
                messageList.appendChild(li);
            });
    
            // ✨ "Yazıyor..." mesajını göster
            socket.on("typing", (user) => {
                typingIndicator.innerText = `${user} yazıyor...`;
            });
    
            // ✨ "Yazıyor..." mesajını kaldır
            socket.on("stop typing", () => {
                typingIndicator.innerText = "";
            });
    
            // ✨ Kullanıcı listesini güncelle
            socket.on("user list", (users) => {
                userList.innerHTML = "";
                users.forEach(user => {
                    let li = document.createElement("li");
                    li.textContent = user;
                    userList.appendChild(li);
                });
            });
        });
    </script>
    
    
    
</head>
<body>
    <h1>You Chat</h1>

    <div class="chat-container">
        <div class="user-list">
            <h3>Online Kullanıcılar</h3>
            <ul id="userList"></ul>

            <p id="typingIndicator" style="font-style: italic; color: gray;"></p>

        </div>
        
        <div class="chat-box">
            <ul id="messages"></ul>
            <p id="typingIndicator" style="font-style: italic; color: gray;"></p>
            <form id="chatForm">
                <input id="msgInput" autocomplete="off">
                <button type="submit">Gönder</button>
            </form>
        </div>
    </div>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Oswald:wght@200..700&family=Roboto+Condensed:ital,wght@0,100..900;1,100..900&family=Splash&display=swap');
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        h1 {
            font-family: 'Splash', cursive;
            color: #8A0707;
        }

        .chat-container {
            display: flex;
            width: 80%;
            margin-top: 20px;
        }

        .user-list {
            width: 20%;
            padding: 10px;
            border-right: 2px solid #ddd;
        }

        .chat-box {
            width: 80%;
            padding: 10px;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            padding: 5px;
            border-bottom: 1px solid #ddd;
        }

        form {
            display: flex;
            margin-top: 10px;
        }

        input {
            flex: 1;
            padding: 5px;
        }

        button {
            padding: 5px 10px;
            margin-left: 5px;
        }

        /* Tablet (Ekran genişliği 1024px veya daha küçükse) */
    @media (max-width: 1024px) {
        .chat-container {
            width: 90%;
        }

        .user-list {
            width: 25%;
        }

        .chat-box {
            width: 75%;
        }
    }

    /* Telefon (Ekran genişliği 600px veya daha küçükse) */
    @media (max-width: 600px) {
        .chat-container {
            flex-direction: column;
            width: 100%;
        }
    
        .user-list {
            width: 100%;
            border-right: none;
            border-bottom: 2px solid #ddd;
            text-align: center;
        }
    
        .chat-box {
            width: 100%;
        }
    
        form {
            flex-direction: column;
            align-items: center; /* Input ve butonu ortalar */
            width: 100%;
        }
    
        input, button {
            width: 80%;
            margin-bottom: 5px;
            box-sizing: border-box; /* Padding ve border genişliği etkilemesin */
        }
    
        button {
            margin-left: 0;
        }
    }
    
    </style>
</body>
</html>
